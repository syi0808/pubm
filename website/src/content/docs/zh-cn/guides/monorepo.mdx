---
title: Monorepo
description: 用正确的顺序和版本策略发布 workspace 包。
---

`pubm` 将 monorepo 视为核心场景，也可以识别 JavaScript、Rust 和 Deno workspace。

## 典型流程

```bash
pubm init --changesets
pubm changesets add
pubm
```
